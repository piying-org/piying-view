---
title: "Advanced Array Usage"
---

import SchemaPreview from '../../../../components/SchemaPreview.astro';
import { SchemaDemoComponent } from '../../../../angular/components/schema-demo.component.ts';

This page covers the advanced array features of Piying-View: add/remove/update operations, deletionMode, groupMode and more.

<SchemaPreview definition="src/angular/definition/array-basic.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="array-basic" />
</SchemaPreview>

## Basic Array Operations

Piying-View's FieldArray provides these methods:

### set — set the value at an index

Call `field.action.set(value, index)` directly:

```typescript
field.action.set('newValue', 0); // set the value at index 0 to 'newValue'
```

Inserting past leading elements is supported:

```typescript
field.action.set('value', 5); // set a value at index 5
```

### remove — remove the element at an index

Call `field.action.remove(index)` directly:

```typescript
field.action.remove(0); // remove the element at index 0
```

### updateValue — replace the whole array

Call `field.form.control.updateValue(value)` directly:

```typescript
field.form.control.updateValue(['a', 'b', 'c']); // replace the whole array content
```

### clear — empty the array

Call `field.form.control.clear()` directly:

```typescript
field.form.control.clear(); // remove every element
```

### reset — reset to the initial value

<SchemaPreview definition="src/angular/definition/array-reset.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="array-reset" />
</SchemaPreview>

## deletionMode — Deletion Mode

Controls what happens when an array element is deleted.

### shrink (default) — Shrink Mode

After a deletion the array length shrinks:

<SchemaPreview definition="src/angular/definition/array-shrink.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="array-shrink" />
</SchemaPreview>

### mark — Mark Mode

After a deletion the slot becomes `undefined` and the length is preserved:

<SchemaPreview definition="src/angular/definition/array-mark.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="array-mark" />
</SchemaPreview>

## groupMode — Group Validation Mode

Controls the validation behaviour of arrays and tuples. `groupMode` is inferred automatically while parsing the schema and never needs to be set.

| Schema type                        | Inferred `groupMode` | Description                   |
| -------------------------------- | ---------------------- | ---------------------------- |
| `v.tuple()`                      | `'default'`            | Fixed-length tuple: items beyond the length are truncated |
| `v.looseTuple()`                 | `'loose'`              | Loose tuple: items beyond the length are kept |
| `v.strictTuple()`                | `'strict'`             | Strict tuple: validation fails when the length is exceeded |
| `v.array()` / the rest part of `v.tupleWithRest()` | `'reset'` | Array / variable tail: no fixed-key or length constraint |

For object groups (`v.object()` / `v.looseObject()` / `v.strictObject()`) see [Type Mapping](en/scenarios/type-mapping/).

### emptyValue — Empty Value

When the array is empty, `emptyValue` is returned as the final value:

<SchemaPreview definition="src/angular/definition/array-empty-value.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="array-empty-value" />
</SchemaPreview>

## Array Properties

The `FieldArray` members `length` / `controls` / `fixedControls$` / `clear()`, together with the field-level `children` / `fixedChildren` / `restChildren`, are control API — see [AbstractControl — FieldArray-specific API](en/api/control-api/#fieldarray-specific-api).

## Tuple and TupleWithRest

### Tuple — Fixed-Length Tuple

<SchemaPreview definition="src/angular/definition/array-tuple.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="array-tuple" />
</SchemaPreview>

### TupleWithRest — Tuple + Variable Tail

<SchemaPreview definition="src/angular/definition/array-tuple-with-rest.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="array-tuple-with-rest" />
</SchemaPreview>

## Full Example

<SchemaPreview definition="src/angular/definition/array-complete.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="array-complete" />
</SchemaPreview>

## Next Steps

- [Record Schema Dynamic Groups](en/scenarios/record-dynamic-group/) — dynamic key/value forms
- [formConfig](en/api/form-config/) — details of deletionMode / groupMode / emptyValue
