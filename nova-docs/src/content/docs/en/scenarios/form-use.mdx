---
title: "Using Forms"
---

import SchemaPreview from '../../../../components/SchemaPreview.astro';
import { SchemaDemoComponent } from '../../../../angular/components/schema-demo.component.ts';

- If you need a certain behaviour and don't know where to start, feel free to [open an issue](https://github.com/piying-org/piying-view/issues)

This page is an overview of form usage. Detailed examples per scenario live in the linked documents; here we show the core "definition is the form" capability plus the common `formHelper` (`__helper`) components.

## Type Mapping

Most `Valibot` types are resolved into form controls; some common ones are listed below. See the [Valibot API](https://valibot.dev/api/) for more types, and use `custom` for your own types.

### string — Text Input

<SchemaPreview definition="src/angular/components/definition/type-string.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="string" />
</SchemaPreview>

### number — Number Input

<SchemaPreview definition="src/angular/components/definition/type-number.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="number" />
</SchemaPreview>

### boolean — Checkbox

<SchemaPreview definition="src/angular/components/definition/type-boolean.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="boolean" />
</SchemaPreview>

### picklist — Select

<SchemaPreview definition="src/angular/components/definition/type-picklist.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="picklist" />
</SchemaPreview>

> For the full control type mapping and interactive demos see [Type Mapping — Definition Is the Form](en/scenarios/type-mapping/).

---

The following types resolve into form groups; each can reference the `formHelper` component through `__helper` to render validation errors and submit/reset buttons:

### object / strictObject — Undefined Keys Filtered Out

<SchemaPreview definition="src/angular/components/definition/form-use-object.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-object" />
</SchemaPreview>

### looseObject — Keeps Undefined Keys

<SchemaPreview definition="src/angular/components/definition/form-use-loose-object.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-loose-object" />
</SchemaPreview>

### objectWithRest — Some Fixed Keys, Some Addable

<SchemaPreview definition="src/angular/components/definition/form-use-object-with-rest.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-object-with-rest" />
</SchemaPreview>

### record — No Fixed Keys, All Keys Addable

> Currently only string keys are supported.

<SchemaPreview definition="src/angular/components/definition/form-use-record.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-record" />
</SchemaPreview>

---

The following types resolve into form arrays:

### tuple / strictTuple — Fixed Items, Not Addable, Extra Values Filtered Out

<SchemaPreview definition="src/angular/components/definition/form-use-tuple.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-tuple" />
</SchemaPreview>

### looseTuple — Fixed Items, Not Addable, Extra Values Kept

<SchemaPreview definition="src/angular/components/definition/form-use-loose-tuple.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-loose-tuple" />
</SchemaPreview>

### tupleWithRest — Some Fixed, Some Addable

<SchemaPreview definition="src/angular/components/definition/form-use-tuple-with-rest.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-tuple-with-rest" />
</SchemaPreview>

### array — Dynamically Added Children

<SchemaPreview definition="src/angular/components/definition/form-use-array.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-array" />
</SchemaPreview>

## Default Values

The second argument of `v.optional()` is the default value, filled into the form on render:

<SchemaPreview definition="src/angular/components/definition/default-value.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="default" />
</SchemaPreview>

## Listening to Value Changes

The `list` property of `valueChange` takes a path or `undefined`, resolving the control through path querying and listening to its value:

<SchemaPreview definition="src/angular/components/definition/form-use-value-change.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-value-change" />
</SchemaPreview>

> More linkage scenarios (hideWhen / disableWhen / cascading / updating other fields) are covered in [Dynamic Field Control](en/scenarios/dynamic-fields/).

## Layout Moves

Data structures often don't match the visual layout; as long as the structure stays valid you can move components. The target position must be parsed first, and larger weights render later:

<SchemaPreview definition="src/angular/components/definition/form-use-layout.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-layout" />
</SchemaPreview>

> See [asControl / asVirtualGroup](en/scenarios/as-control-group/) for detailed examples.

## Section Disabling

When parts of a form may or may not be editable depending on the context, use `disableWhen`:

<SchemaPreview definition="src/angular/components/definition/form-use-disable-when.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-disable-when" />
</SchemaPreview>

## Arrays

You can always wrap a type definition in `v.array()` to make it an array:

<SchemaPreview definition="src/angular/components/definition/form-use-array-rw.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-array-rw" />
</SchemaPreview>

> Advanced usage (add/remove/update, deletionMode, groupMode) is covered in [Advanced Array Usage](en/scenarios/array-advanced/).

## Nested Definitions

Use `object` for multi-level data structures; they render as nested child form groups automatically:

<SchemaPreview definition="src/angular/components/definition/form-use-fieldset.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-fieldset" />
</SchemaPreview>

## Field Grouping

`v.intersect()` + `asVirtualGroup` supports multi-level virtual groups, combining with a `tabs` component for tabbed groups:

<SchemaPreview definition="src/angular/components/definition/form-use-tabs-group.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-tabs-group" />
</SchemaPreview>

## Updating Other Fields on Value Change

Listen to controls with `valueChange` and update the values of other controls:

<SchemaPreview definition="src/angular/components/definition/form-use-update-other.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-update-other" />
</SchemaPreview>

## Validation

Validation is fully decoupled from control components: you implement it yourself and only get control states. Wrap a layer with `wrapper`, or use a group dedicated to validation:

<SchemaPreview definition="src/angular/components/definition/form-use-validator.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-validator" />
</SchemaPreview>

<SchemaPreview definition="src/angular/components/definition/form-use-valid-group.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-valid-group" />
</SchemaPreview>

> Synchronous/asynchronous validators and formConfig.validators are covered in [Custom Validation](en/scenarios/custom-validation/).

## Cascading

Listen to specific controls with `valueChange` and update their values. The first emission defaults to `undefined` (when no default value is set) and must be skipped:

<SchemaPreview definition="src/angular/components/definition/form-use-cascade.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-cascade" />
</SchemaPreview>

## Filter Group

Create a custom form group that controls the visibility of its inner fields (use the `filterGroup` component for large numbers of fields):

<SchemaPreview definition="src/angular/components/definition/form-use-filter-group.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-filter-group" />
</SchemaPreview>

> Interactive example: [Advanced Array Usage — Filter Group](en/scenarios/array-advanced/#filter-group).

## Scroll Group

Create controls lazily with `defer` and combine them with the `scrollGroup` component for scroll-based lazy loading of many fields:

<SchemaPreview definition="src/angular/components/definition/form-use-scroll-group.definition.ts">
  <SchemaDemoComponent slot="preview" client:only="angular" name="form-use-scroll-group" />
</SchemaPreview>

> Interactive example: [Advanced Array Usage — Scroll Group](en/scenarios/array-advanced/#scroll-group).

## Next Steps

- [Type Mapping — Definition Is the Form](en/scenarios/type-mapping/) — more types, default values and nested definitions
- [Dynamic Field Control](en/scenarios/dynamic-fields/) — valueChange / disableWhen / cascading
- [asControl / asVirtualGroup](en/scenarios/as-control-group/) — layout moves / field grouping
- [Advanced Array Usage](en/scenarios/array-advanced/) — filter group / scroll group
- [Custom Validation](en/scenarios/custom-validation/) — validators and group validation
